# Plotting and comparison scripts

Place the final SCHISM post-processing notebook or Python modules here after the river-boundary correction and six-day comparison are complete.

Required standards:

- shared timestamps and colour limits with TUFLOW;
- explicit variable names and units;
- common surface/near-bed/depth-averaged definitions;
- geographic interpolation rather than matching cell or node IDs;
- reproducible command-line or notebook workflow.
